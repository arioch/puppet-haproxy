frontend <%= @name %>
    mode            <%= @mode %>
    bind            <%= @bind %>
    default_backend <%= @default_backend %>

<% if @acl -%>
    acl <%= @acl %> hdr(host) -i <%= @acl_header %>
    use_backend <%= @acl_backend %> if <%= @acl %>
<% end -%>

